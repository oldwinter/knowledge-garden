---
alwaysApply: true
---
# 发布流程（Quartz Syncer）

- 发布基于 frontmatter 中的 `publish: true` 标记。
- 现存入口：Obsidian [[quartz syncer]] 的 `publication center`。详见 [🌏 本库发布指南.md](mdc:🍀 花园导览/🧰 本库指南/🌏 本库发布指南.md)。
- 根目录没有 `publish_by_frontmatter.py`。不要运行 `python publish_by_frontmatter.py`，也不要新写会 git push 的发布脚本。

## 流程摘要
1. 给要发布的笔记打上 `publish: true`。
2. 在 Obsidian 里执行 Quartz Syncer 的 `publication center`。
3. 插件把文件复制到 dg3 仓库 content 目录，Netlify 更新 https://garden.oldwinter.top。

## 注意事项
- 使用 `.gitignore` 过滤敏感文件。
- 发布前请检查隐私信息、草稿状态与失效链接。
